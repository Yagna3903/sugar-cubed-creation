// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:SupaBase1@db.idsfkvwjaiwadpitgjnu.supabase.co:5432/postgres")
}

// Example model: Products
// You’ll add more later based on your app needs
model Product {
  id          String   @id @default(cuid())
  name        String
  description String?
  price       Float
  imageUrl    String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
